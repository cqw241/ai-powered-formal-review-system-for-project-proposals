# W3 接线与验收记录

分支：`feat/w3-review-integration`  
记录日期：2026-09-16  
范围：把 B07/B08/B09 执行器接入 B06 审查工作台，同一任务运行 RULE-002/003/004/005/006/007/010（**不**实现 B10 缺件、B11 双文档对照页、B12 人工处置）

## 1. 基线

| 项 | 事实 |
|---|---|
| 交接基线 | `main`（B06 + W3 契约，`1de0780`）+ PR #9/#10/#11 领域执行器 |
| 工作分支 | `feat/w3-review-integration` |
| 未做 | 推送合并前的独立 Review、RULE-001/008/009、登录、OCR、任务队列 |

## 2. 接线方式

- 内置条目：RULE-002/003/004/006/007/010，始终纳入任务。
- 勾选的已启用规则：有执行器则运行（RULE-005 用当时版本快照）；无执行器仍标未执行。
- 每个条目调用对应 `execute_*`，结果经 `persist_rule_result` 写入 `review_item_results` 并同步工作台状态。
- RULE-007 仍复用 `run_funding_review`，同时转成 `RuleExecutionResult` 以便展示证据。
- 工作台点击证据打开对应材料页（复用既有页预览/高亮，不是 B11 双文档页）。

## 3. 验收对照

| 验收项 | 结果 | 证据 |
|---|---|---|
| PKG-A 名称/周期/上限/申请经费 | **通过** | `test_pkg_a_one_review_passes_name_period_funding_and_cap`：002/003/004/005/007/010 均为 PASS |
| PKG-B 名称差异、超上限、晚结束/晚签署 | **通过** | `test_pkg_b_lists_name_diff_over_cap_and_late_dates`：002 FAIL 含双方摘录；005 153000 vs 150000；004 2029-02-28；010 2026-10-02 |
| PKG-C 预算合计 286000、负责人一致 | **通过** | `test_pkg_c_budget_sum_and_principal_pass` |
| 刷新后仍在 | **通过** | GET `/reviews/{id}` 仍为 PASS |
| 合计差额与类别缺失可区分 | **通过** | RULE-006 差额 2 元 FAIL；类别缺失 RULE-005 待确认 |
| 点击双方原文 | **通过** | 浏览器 PKG-B：RULE-002 列出申报书/预算/承诺书摘录；点 B1 与 B3 分别打开对应页预览 |
| 后端测试 | **通过** | `uv run pytest -q` → 161 passed |

B10 应新增缺件与条件附件；不要在本分支补这些行为。
